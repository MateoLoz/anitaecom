export const LogoSection = ({ size }) => {
    return (
        <>
         <section className="logo-section">
            <img src="./src/mocks/Logo.png" alt="" className="logo-img" style={{ width: size, height: size }} />
        </section>
        </>
    )
}